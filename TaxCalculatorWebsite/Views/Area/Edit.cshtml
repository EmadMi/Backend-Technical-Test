@model Application.DTOs.Areas.UpdateAreaDTO

@{
    ViewData["Title"] = "Edit";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}

<form asp-action="Edit">
    <div class="row">
        <div class="col-12 py-3 bg-secondary bg-opacity-10 text-end rounded-3 d-flex justify-content-between align-items-center">
            <a class="btn btn-warning" asp-action="Index">Back To List</a>
            <p class="mb-0 bg-danger text-light px-2 badge @(TempData["EditError"] == null ? "invisible":"")">
                @TempData["EditError"]
            </p>
            <input type="submit" value="Edit" class="btn btn-primary px-5" />
            <input asp-for="Id" type="hidden" />
        </div>
        <div class="col-12 py-3">
            <div class="row">
                <div class="col-11 col-md-6 col-lg-4 mx-auto rounded-3 p-3 border shadow shadow-sm">
                    <div class="row px-0 mx-0">
                        <div class="col-12 d-flex flex-column bg-light rounded-top py-2">
                            <div class="form-floating">
                                <input asp-for="Name" class="form-control" placeholder=" " />
                                <label asp-for="Name" class="text-muted small"></label>
                                <span asp-validation-for="Name" class="text-danger small"></span>
                            </div>
                        </div>
                        <div class="col-12 d-flex flex-column bg-secondary bg-opacity-10 py-2">
                            <div class="form-floating">
                                <input asp-for="MaxTaxFee" min="0" class="form-control" type="number" placeholder=" " />
                                <label asp-for="MaxTaxFee" class="text-muted small"></label>
                                <span asp-validation-for="MaxTaxFee" class="text-danger small"></span>
                            </div>
                        </div>
                        <div class="col-12 d-flex flex-column bg-light rounded-top py-2">
                            <div class="form-floating">
                                <input asp-for="Order" type="number" min="-1" placeholder=" " class="form-control" />
                                <label asp-for="Order" class="text-muted small"></label>
                                <span asp-validation-for="Order" class="text-danger small"></span>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</form>
